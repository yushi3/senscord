<?xml version="1.0" encoding="UTF-8"?>
<!-- Sample server configuration. -->
<server client="off">
  <listeners>
    <listener
        connection="tcp"
        address="127.0.0.1:8080"
        addressSecondary="127.0.0.1:8081" />
    <!--
    <listener
        connection="ws"
        address="127.0.0.1:65000"
        addressSecondary="127.0.0.1:65010" />
    -->
    <!--
    <listener
        connection="uds"
        address="@/tmp/senscord"
        addressSecondary="@/tmp/senscord.2" />
    -->
    <!--
    <listener
        connection="ucom"
        address="0:65000" />
    -->
  </listeners>

  <streams>
    <defaults>
      <frame buffering="off"/>
    </defaults>
  </streams>
</server>
